```tsx
import { MiniLogger } from './MiniLogger';

<MiniLogger
    play={() => {}}
    pause={() => {}}
    continue={() => {}}
    getTime={() => ['23', 0.4]}
    isFocusing={true}
    task="完成 MiniLogger 功能"
    style={{ width: 230 }}
/>;
```
